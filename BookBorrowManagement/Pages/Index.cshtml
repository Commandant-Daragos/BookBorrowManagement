@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Intro page for example...Automatically generated...</p>
    <p>
        <a asp-page="/Books/Index">See all books</a>
    </p>
    <p>
        <a asp-page="/Books/Create">Create New Book</a>
    </p>
    <form method="get" action="/Books/Delete">
        <p>Delete book by ID</p>
        <select asp-items="Model.BookIDs" name="id"> @* asp-for="SelectedBookId" *@
            <option value="">--</option>
        </select>
        <div class="form-group">
            <input type="submit" value="Delete" class="btn btn-primary" />
        </div>
    </form>
</div>
